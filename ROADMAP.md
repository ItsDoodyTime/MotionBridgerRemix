# The current to-do list before initial release:

- Add eye blink detection by threshold
- Add body tilt detection
- Add PNGRemix action mapping per sprite
- Add auto-copy error log button
- Add self-check diagnostics before launching
- Add profile system (switching between different models with different values)

## Planned for future updates:
- Add hand tracking
- Add setup wizard to simplify and streamline configuration of the tool
- Add shutdown confirmation
- Optimize and let program run in background, add a tray icon.
